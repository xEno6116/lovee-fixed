# Multi-site Access Design

ระบบจะจัดเก็บเว็บไซต์แต่ละรายการในตาราง `anniversary_sites` โดยมี `ownerId` เป็นเจ้าของโดยตรง ข้อมูลการตั้งค่าและสื่อทุกชิ้นจะมี `siteId` เพื่อป้องกันการปะปนระหว่างเว็บไซต์

> เส้นทาง backend ทุกเส้นที่จะอ่านหรือแก้ไขข้อมูลเว็บไซต์จะตรวจว่า `ownerId` ของเว็บไซต์ตรงกับผู้ใช้ที่ล็อกอินอยู่ก่อนเสมอ

| ข้อมูล | การแยกข้อมูล | สิทธิ์เข้าถึง |
|---|---|---|
| เว็บไซต์ | `anniversary_sites.ownerId` | เจ้าของที่ล็อกอินเท่านั้น |
| การตั้งค่าและ PIN | `site_settings.siteId` | เจ้าของของเว็บไซต์นั้นเท่านั้น |
| รูป วิดีโอ และเพลง | `media_assets.siteId` และ S3 prefix ตามเว็บไซต์ | เจ้าของของเว็บไซต์นั้นเท่านั้น |

หน้า `/dashboard` จะใช้สร้างและเลือกรายการเว็บไซต์ ส่วนหน้า `/site/:slug` และ `/site/:slug/settings` จะเป็นเว็บไซต์และหน้าจัดการของรายการที่เลือก โดยทั้งสองหน้าต้องผ่านการตรวจสิทธิ์ของเจ้าของ
