# Verification Notes

## Public page — 19 August 2026

- PIN `0000` ปลดล็อกหน้าสาธารณะได้สำเร็จ
- หน้าหลังปลดล็อกแสดงตัวนับเวลา, ช่องวิดีโอ 4 ช่องพร้อมปุ่มเลื่อน, แกลเลอรี่, ส่วนข้อความ, birthday card และตัวเล่นเพลง
- birthday card แสดงไฟล์เค้กจาก `/manus-storage/legacy-birthday-cake_645e071a.gif` และมีปุ่ม `เป่าเทียน`
- บัญชีผู้เยี่ยมชมไม่มีช่อง GitHub Token บนหน้าสาธารณะ; การจัดการข้อมูลอยู่หลังระบบเจ้าของ

## Settings page — 19 August 2026

หน้า Settings แสดงช่องวิดีโอครบ 4 ช่อง, อัปโหลดรูปภาพและเพลง, ฟอร์มข้อความ วันเริ่มคบ PIN และข้อความวันเกิด โดยไม่มีช่อง GitHub Token การบันทึกค่าปัจจุบันผ่านหน้า Settings สำเร็จและแสดงข้อความยืนยันว่าเว็บไซต์สาธารณะจะใช้ข้อมูลล่าสุดทันที

การทดสอบอัปโหลดรูปภาพผ่านหน้า Settings สำเร็จ โดยระบบแจ้งผลว่าอัปโหลดไฟล์ไปยังพื้นที่จัดเก็บเรียบร้อยแล้วและแสดงรายการสื่อทันที จากนั้นลบไฟล์ทดสอบผ่านปุ่มลบในหน้าเดียวกันสำเร็จ และรายการกลับเป็นสถานะไม่มีรูปภาพตามเดิม

เพื่อทดสอบการอัปเดตค่าต้นทางแบบ end-to-end ได้บันทึกข้อความชั่วคราวผ่าน Settings สำเร็จแล้ว ขั้นตอนถัดไปคือเปิดหน้าสาธารณะเพื่อตรวจว่าข้อความนี้มาจาก backend จริง ก่อนคืนข้อความเดิมให้เว็บไซต์

หน้าสาธารณะหลังปลดล็อกด้วย PIN `0000` แสดงข้อความชั่วคราวที่บันทึกผ่าน Settings ได้จริง จึงยืนยันเส้นทาง Settings → Backend API → Public page สำเร็จ และต้องคืนข้อความเดิมว่า `บันทึกความทรงจำของเรา` ก่อนส่งมอบ

ได้คืนค่า `บันทึกความทรงจำของเรา` ผ่านหน้า Settings สำเร็จและยืนยันข้อความในฟอร์มหลังบันทึกแล้ว

การทดสอบสื่อแบบ end-to-end ยืนยันว่าไฟล์ GIF ที่อัปโหลดผ่าน Settings ถูกบันทึกใน S3 และแสดงจริงในส่วนแกลเลอรี่ของหน้า public หลังปลดล็อกด้วย PIN จากนั้นลบไฟล์ทดสอบออกผ่านหน้า Settings สำเร็จ ทำให้แกลเลอรี่กลับสู่สถานะไม่มีรูปภาพตามเดิม

## Mobile public page — 19 August 2026

การทดสอบใน viewport ขนาดมือถือยืนยันว่า PIN `0000` ปลดล็อกหน้าสาธารณะได้ และการกดปุ่มวิดีโอถัดไปทำให้ลำดับเปลี่ยนจาก `1/4` เป็น `2/4` ตามปกติ โดยหน้าจอแสดงตัวนับเวลา วิดีโอ แกลเลอรี่ CD player และ birthday card ได้ครบถ้วน

มีการอัปโหลดรูปภาพทดสอบเพื่อยืนยันแกลเลอรี่บนมือถือเพิ่มเติม ผลการตรวจพบว่าแท็กภาพโหลดสำเร็จครบถ้วนที่ขนาดต้นฉบับ `480 × 480` และขนาดที่แสดง `297 × 297` พิกเซล ก่อนลบสื่อทดสอบคืนสภาพข้อมูล นอกจากนี้ desktop หลังปลดล็อกด้วย PIN สามารถเลื่อนวิดีโอจาก `1/4` เป็น `2/4` ได้จริง

## Final checks

การตรวจ TypeScript, Vitest จำนวน 7 รายการ และ production build ผ่านทั้งหมดแล้วเมื่อ 19 August 2026
